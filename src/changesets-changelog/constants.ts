export const defaultLineTemplate = '* {pull+}{commit+}{(users)+}{lines}';
export const invalidOptionsErrorMessage = 'Please provide a repo to this changelog generator like this:\n"changelog": ["@reallyland/changelog-github", { "repo": "org/repo" }]' as const;
